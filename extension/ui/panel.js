/**
 * Panel UI - placeholder module loaded by manifest.
 * All panel logic (injection, rendering, show/hide) lives in content.js
 * since it needs direct DOM access to the Gmail page.
 *
 * This file is intentionally minimal — it exists so the manifest's
 * content_scripts entry resolves without error.
 */
